sipeed_tof_node:
  ros__parameters:
    # The serial device path for the ToF camera
    device: "/dev/ttyUSB0"
    
    # The frame_id for published messages
    frame_id: "tof_camera_link"
    
    # The period for the main processing timer in milliseconds
    timer_period_ms: 30